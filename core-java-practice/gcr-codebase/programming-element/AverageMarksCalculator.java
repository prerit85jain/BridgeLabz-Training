public class AverageMarksCalculator {
    // Main Function
    public static void main(String[] args) {
        // Marhs obtained in PCM
        int maths = 94;
        int physics = 95;
        int chemistry = 96;

        // Calculate Average
        double average = (maths + physics + chemistry) / 3;
        System.out.println("Sam's average mark is PCM is " + average);
    }
}
